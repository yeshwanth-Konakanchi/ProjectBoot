package com.nivtek.training.assignment;

public class SortThreeNumbers {

  /**
   * @param args
   */
  public static void main(String[] args) {
    /*
     * Plan of action or pseudo code
     * -----------------------------
     * Convert string args[0] to int a
     * Convert string args[1] to int b
     * Convert string args[2] to int c
     * 
     * print a and b and c
     * 
     * a = 2, b = 5, c = 1
     * 
     * if a > b {
     *    exchange a and b
     *    int temp = a;
     *    a = b;
     *    b = temp;
     * }
     * 
     * print a and b and c
     * a = 2, b = 5, c = 1
     * 
     * if b > c {
     *    exchange b and c
     *    int temp = b;
     *    b = c;
     *    c = temp;
     * }
     * 
     * print a and b and c
     * a = 2, b = 1, c = 5
     * 
     * if a > b {
     *    exchange a and b
     *    int temp = a;
     *    a = b;
     *    b = temp;
     * }
     * 
     * print a and b and c
     * a = 1, b = 2, c = 5
     */


  }

}
