package com.nivtek.training.string;

public class ClassAssignmentString {

  /**
   * @param args
   */
  public static void main(String[] args) {
    String s, t, h, a;
    String n, e;
    int i;
    h = "Hello";
    s = "  How are you?  ";
    a = "abc";
    n = null;
    e = "";

    System.out.println(h.length());
    System.out.println(h.substring(1));
    System.out.println(h.toUpperCase());
    System.out.println(h.toUpperCase().toLowerCase());
    System.out.println(h.indexOf("Y"));
    System.out.println(h.startsWith("ell"));
    System.out.println("Tomorrow".indexOf("o"));
    System.out.println("Tomorrow".indexOf("o",7));
    
    System.out.println("a = \"" + a + "\"");
    System.out.println((a.length() + a).startsWith("a"));
    
    System.out.println(">>>" + a.length() + a.startsWith("a"));
    
    System.out.println(a.substring(1,3).equals("bc"));
    System.out.println(a.substring(1,3) == "bc");
    
    System.out.println("a".compareTo("A"));
    System.out.println(s.trim().charAt(2));
    
    // the primitive boolean true is converted to the wrapper Boolean object containing true
    // The Boolean containing true is converted to the string "true" via
    // the toString method on the Boolean object
    // every primitive type has a corresponding wrapper object
    // for eg, int primitive type has an Integer wrapper object
    //         float will have Float
    //         boolean will have Boolean
    // System.out.println(a.length() + Boolean.valueOf(a.startsWith("a")).toString());
  }

}
